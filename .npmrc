; Ensure devDependencies (eslint/typescript/etc.) are installed even if CI sets NODE_ENV=production.
; Without this, `npm ci` may omit dev deps, causing `npm run lint` / `next build` to fail.
production=false
omit=optional

